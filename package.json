{
  "name": "blockfront-server",
  "version": "1.0.0",
  "description": "BLOCKFRONT — serves the game and runs multiplayer (lobbies, co-op, free-for-all, duels, accounts) from one Node process.",
  "main": "server.js",
  "engines": { "node": ">=18" },
  "scripts": { "start": "node server.js" },
  "dependencies": { "socket.io": "^4.8.1" }
}
